'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
import {apiGet} from '../../lib/api';
export default function Creator(){const [data,setData]=useState<any>(null);const [error,setError]=useState('');useEffect(()=>{apiGet('/api/analytics/creator').then(setData).catch(e=>setError(e.message))},[]);if(error)return <main className="auth-page"><div className="auth-card"><p className="error">{error}</p><Link href="/">← Feed</Link></div></main>;if(!data)return <main className="auth-page"><div className="auth-card">Loading analytics…</div></main>;return <main className="feed"><div className="hero"><h1>Creator Studio 📊</h1><Link href="/">← Feed</Link></div><section className="stats"><div>Followers<strong>{data.followers}</strong></div><div>Views<strong>{data.totals.views}</strong></div><div>Likes<strong>{data.totals.likes}</strong></div><div>Comments<strong>{data.totals.comments}</strong></div><div>Shares<strong>{data.totals.shares}</strong></div></section><h2>Your videos</h2><div className="table-wrap"><table><thead><tr><th>Video</th><th>Views</th><th>Likes</th><th>Comments</th><th>Shares</th></tr></thead><tbody>{data.videos.map((v:any)=><tr key={v.id}><td>{v.title}</td><td>{v.views}</td><td>{v.likes}</td><td>{v.comments}</td><td>{v.shares}</td></tr>)}</tbody></table></div></main>}
